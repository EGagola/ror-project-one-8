<%= form_with(model: outer, local: true) do |form| %>
  <% if outer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(outer.errors.count, "error") %> prohibited this outer from being saved:</h2>

      <ul>
      <% outer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
